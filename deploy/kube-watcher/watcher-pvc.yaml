apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pulsar-admin-client-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard  # Adjust based on your cluster's storage classes

